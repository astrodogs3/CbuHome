# SQLite version 3.x
#   gem install sqlite3
development:
  adapter: sqlite3
  database: db/cbuwork_development.sqlite
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

production:
  adapter: adapter: postgresql
  database: db/cbuwork_production  #not sure if correct here
  pool: 5
  timeout: 5000